Generate a weekly (7-day) named with week days ex("Monday","Friday"...) personalized diet plan based on the following daily nutritional targets and user preferences:

Daily Target:  
Macros = {{macros}}

User Preferences & Restrictions:
- Gender: {{gender}}
- Age: {{age}}
- Height: {{height}} cm
- Weight: {{weight}} kg
- Activity Level: {{activity_level}}
- Goal: {{goal}}
- goal_description : {{goal_description}}
- Goal Speed: {{goal_speed}}
- Diet Style: {{diet_style}}
- Dietary Restrictions: {{dietary_restrictions}}
- Meals Per Day: {{meals_per_day}}
- Budget Level: {{budget_level}}
- Fasting: {{fasting_enabled}}
- Goal Reason: {{goal_reason}}
- Goal detailed description": "{{goal_description}}",

Important Instructions:

- Distribute the macros evenly across the {{meals_per_day}} meals per day.
- dont include foods like {{food_you_dont_want}}
- Keep meals realistic and aligned with the goal and restrictions.
- Use only ingredients allowed by the user (e.g., halal, gluten-free if specified).
- Ensure meals are budget-conscious (based on {{budget_level}}).
- For each ingredient, include an approximate **measurement in grams (g), milliliters (ml), or pieces**.
- Return output in a structured JSON format with 7 objects (one per day).
- Ensure all macro values are based on realistic nutritional data per 100g or ml and match actual food databases.
- Each day must contain {{meals_per_day}} meals. For each meal, include:
  - "meal_type": (e.g., "breakfast", "lunch", etc.)
  - "description": (short description of the meal)
  - "ingredients": [array of ingredient objects with "name" "unit" , "calories" ,"amount" (e.g.,   "ingredients": [
    {
      "name": "Chicken Breast",
      "amount": 150,
      "unit": "g",
      "calories": 165
    },)]
  - "calories": number
  - "protein": number
  - "carbs": number
  - "fat": number

Do not include preparation steps or how to cook the meals. Focus only on structured data and nutrient content per meal.

At the end of the JSON, include a "grocery_list" object that lists all ingredients used across all 7 days and meals, with **total quantities** in grams, ml, or pieces, using this format:

"grocery_list": {
  "eggs": "10 pieces",
  "chicken breast": "1.2 kg",
  "brown rice": "700g",
  ...
}
"Create two arrays based on a 7-day meal plan:
An array called diet_plan that contains each day's meals (breakfast, lunch, dinner, and snacks if needed) for 7 days.
An array called grocery_list that includes all the ingredients needed to prepare the meals in the diet plan, with quantities if possible."
please return only the JSON of the diet plan — no additional calculations or explanations. You can do calculations under the hood, but I want only the JSON as the result.
